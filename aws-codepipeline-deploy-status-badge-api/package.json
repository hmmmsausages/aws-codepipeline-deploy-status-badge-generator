{
  "name": "aws-codepipeline-deploy-status-badge-generator-api",
  "version": "1.0.0",
  "description": "Generates GitHub badges based on DynamoDB data and serves it via Rest API",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hmmmsausages",
  "dependencies": {
    "badgen": "^3.1.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.656.0",
    "standard": "^14.3.4"
  }
}
